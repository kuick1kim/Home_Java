

#합집합 교집합 알아보기

# 합집합 |
# 교집합 &

english = c("A","B","C")
math = c("C","B","A")

grade_a1 = ifelse(english == "A" | math =="A", "A", "Else")
grade_a1

grade_a2 = ifelse(english == "A" | math =="A", "A", "Else")
grade_a2

grade_a3 = ifelse(english == "B" | math =="B", "A", "Else")
grade_a3

